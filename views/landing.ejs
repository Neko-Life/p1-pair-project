<!DOCTYPE html>

<html>
<head></head>
<body>
<%- include("partials/navbar", { user, profile }) %>

<section class="body content">
  <% if (user?.id) { %>
    <%- include("partials/order", { errors, user, profile, drivers }) %>
  <% } else { %>
    <%- include("partials/login", { errors } ) %>
   <% } %>
</section>
</body>
</html>
