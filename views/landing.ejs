<!DOCTYPE html>

<html>
<head></head>
<body>
<%- include("partials/navbar", { user }) %>

<section>
  <% if (user) { %>
    <%- include("partials/order", { errors, user }) %>
  <% } else { %>
    <%- include("partials/login", { errors, user } ) %>
   <% } %>
</section>
</body>
</html>
